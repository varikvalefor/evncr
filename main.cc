#include "hi.h"
#include "hv.h"
#include "hf.h"

int main(int argc, char * argv[])
{
	for(int main_1 = 1;   main_1 < argc;   main_1++)
		fmp5(argv[main_1]);
	char vaaa[512];
	fgets(vaaa, sizeof vaaa, stdin);
	return fred(vaaa);
}
